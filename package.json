{
  "name": "zod-mermaid",
  "version": "0.1.0",
  "description": "A TypeScript library for working with Zod schemas and Mermaid diagrams",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "type-check": "tsc --noEmit",
    "prepare": "npm run build"
  },
  "keywords": [
    "typescript",
    "zod",
    "mermaid",
    "schema",
    "validation"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.0.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-plugin-jest": "^27.4.3",
    "eslint-plugin-prettier": "^5.0.1",
    "jest": "^30.0.0",
    "prettier": "^3.1.0",
    "rimraf": "^6.0.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "zod": "^4.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
